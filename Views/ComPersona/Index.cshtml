@model IEnumerable<Cuidados.Caninos.Marcos.Montiel.Models.ComPersona>
@{
    ViewData["Title"] = "Persona";
}

<link rel="stylesheet" type="text/css" href="~/css/tablas.css" />
<link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.13/css/all.css"
      integrity="sha384-DNOHZ68U8hZfKXOrtjWvjxusGo9WQnrNx2sqG0tfsghAvtVlRW3tvkXWZh58N9jp"
      crossorigin="anonymous">

<div class="contenedor-formulario">
    <div class="titulo">
        <h2 class="titulo2">Persona</h2>
    </div>
    <div class="wrap">
        <div class="form-group">
            <form asp-action="Index" method="get">
                <a asp-action="Create" class="btn btn-default agregar"><i class="fas fa-plus"></i> Agregar</a>
                <input type="text" class="search-text" maxlength="20" name="searchPerson" value="@ViewData[" CurrentFilter"]" />
                <button type="submit" class="btn btn-default buscar"><i class="fas fa-search"></i> Buscar</button>
                <a asp-action="Index" class="btn btn-default limpiar"><i class="fas fa-search-minus"></i> Limpiar</a>
            </form>
        </div>
        <div class="table-design">
            <table>
                <thead class="table-head">
                    <tr>
                        <th><a asp-action="Index" asp-route-valuesOrder="@ViewData["NameOrderAscDesc"]">Nombre</a></th>
                        <th><a asp-action="Index" asp-route-valuesOrder="@ViewData["APatOrderAscDesc"]">Apellido Paterno</a></th>
                        <th><a asp-action="Index" asp-route-valuesOrder="@ViewData["AMatOrderAscDesc"]">Apellido Materno</a></th>
                        <th>Género</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                @foreach (var item in Model)
                {
                    <tr class="table-body">
                        <td>
                            @Html.DisplayFor(modelItem => item.Nombre)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.APaterno)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.AMaterno)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ComCatSexo.Nombre)
                        </td>
                        <td>
                            <a asp-action="Details" asp-route-id="@item.ID" class="detalle"><i class="fas fa-eye"></i></a>
                            <a asp-action="Edit" asp-route-id="@item.ID" class="editar"><i class="fas fa-edit"></i></a>
                            <a asp-action="Delete" asp-route-id="@item.ID" class="eliminar"><i class="fas fa-trash-alt"></i></a>
                        </td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    </div>
</div>