(function() {     var formulario = document.formulario_registro,         elementos = formulario.elements;          /*Functions*/         var validarInputs = function() {             for (var i = 0; i < elementos.length; i++) {                 if (elementos[i].type == "text" || elementos[i].type == "date" || elementos[i].id == "drop") {                     if (elementos[i].value.length == 0) {                         elementos[i].className = elementos[i].className + " error";                         return false;                     } else {                         elementos[i].className = elementos[i].className.replace(" error", "");                     }                 }             }             return true;         };          var enviar = function(e) {             if (!validarInputs()) {                 e.preventDefault();             }         };          //Functions Blur & Focus         var focusInput = function() {             this.parentElement.children[1].className = "label active";             this.parentElement.children[0].className = this.parentElement.children[0].className.replace("error", "");         };          var blurInput = function() {             if (this.value <= 0) {                 this.parentElement.children[1].className = "label";                 this.parentElement.children[0].className = this.parentElement.children[0].className + " error";             }         };          //Events         formulario.addEventListener("submit", enviar);          for (var i = 0; i < elementos.length; i++) {             if (elementos[i].type == "text" || elementos[i].type == "date" || elementos[i].id == "drop") {                 elementos[i].addEventListener("focus", focusInput);                 elementos[i].addEventListener("blur", blurInput);             }         } }())